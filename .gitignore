# archives (extracted contents are tracked instead where useful)
*.zip

# NOT for the public repo: unpublished manuscript, internal planning, legacy collaboration code
# (repo hlammiv/hadronic_tensor is PUBLIC — keep these local until decided otherwise)
paper/
PLAN.md
z2_matter_qc-master/

# python
__pycache__/
*.pyc
.ipynb_checkpoints/
*.egg-info/
.venv/
venv/

# OS cruft
.DS_Store

# large legacy run data — keep locally, don't ship to GitHub
z2_matter_qc-master/simulation_main/rundata/*.pkl
z2_matter_qc-master/simulation_main/*.csv
z2_matter_qc-master/pauli_twirling/*.txt

# scratch outputs
*.log
scratch/

# internal reporting (management/admin) -- not for the public repo
reports/

# not for the public repo: award paperwork (signatures), third-party papers,
# collaborator review material, internal planning, scratch
# campaign/  - award letter (signatures), run logs, internal reports, scratch
# archive/   - superseded drafts, old plans, third-party papers
# notes/feedback/ - collaborator review comments
# data/work/ - working caches and checkpoints (prep MPS, kappa ensemble, ckpts, dry runs)
# paper/annotations/ - annotated PDFs from collaborators
campaign/
archive/
notes/feedback/
data/work/
paper/annotations/
*.xml
